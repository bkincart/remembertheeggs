import React, { Component } from 'react';

class GroceryFormContainer extends Component {
  constructor(props) {
    super(props);

    this.state = {
      
    }
  }

  render() {
    return(
      //FORM includes:
      // name text box
      // quantity text box
      //submit button that updates the groceries database
    )
  }
}

export default GroceryFormContainer;
