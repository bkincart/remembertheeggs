import React, { Component } from 'react';

class InventoryFormContainer extends Component {

  render() {
    return(
      //FORM includes:
      // dropdown using this.props.groceries and iterating through to grab 'name(unit)'
      // quantity text box
      // dropdown using this.props.locations 
      //submit button that updates the inventories database
    )
  }
}

export default InventoryFormContainer;
